@model IEnumerable<EmployeeManagement_TAsk1.Models.Employee>

    @{
        ViewData["Title"] = "Employee List";
    }

    <h2 class="text-primary text-center">Employee List</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>

    <div class="row">
        <div class="col-md-6">
        </div>
    </div>

    <br />

    <table id="employeeTable" class="table table-bordered table-striped table-responsive">
        <thead>
            <tr>
                <th class="sortable" data-column="FirstName">First Name</th>
                <th class="sortable" data-column="LastName">Last Name</th>
                <th class="sortable" data-column="Email">Email</th>
                <th class="sortable" data-column="Mobile">Mobile</th>
                <th class="sortable" data-column="Address">Address</th>
                <th class="sortable" data-column="Actions">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                    <td>@Html.DisplayFor(modelItem => item.LastName)</td>
                    <td>@Html.DisplayFor(modelItem => item.Email)</td>
                    <td>@Html.DisplayFor(modelItem => item.Mobile)</td>
                    <td>@Html.DisplayFor(modelItem => item.Address)</td>
                    <td>
                        @Html.ActionLink("Update", "Update", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @section scripts {
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.datatables.net/1.10.25/js/jquery.dataTables.min.js"></script>
        <script>
            $(document).ready(function () {
                // Initialize DataTable
                $('#employeeTable').DataTable({
                    lengthMenu: [[5, 10, 25, -1], [5, 10, 25, "All"]],
                    order: [],
                    columnDefs: [
                        { orderable: false, targets: ['actions'] }
                    ],
                });

                // Add event listener for the filter input
                $('#filterInput').on('input', function () {
                    var value = $(this).val().toLowerCase();
                    $('#employeeTable').DataTable().search(value).draw();
                });
            });
        </script>
    }





@*@model IEnumerable<EmployeeManagement_TAsk1.Models.Employee>

    <h1>Employee List</h1>

    <table id="employeeTable" class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Mobile</th>
                <th>Address</th>
                <th>Action</th>
            </tr>
        </thead>
    </table>

    <script>
        $(document).ready(function () {
            $('#employeeTable').DataTable({
                "processing": true,
                "serverSide": true,
                "ajax": {
                    "url": "/Employee/LoadData",
                    "type": "POST"
                },
                "columns": [
                    { "data": "id" },
                    { "data": "firstName" },
                    { "data": "lastName" },
                    { "data": "email" },
                    { "data": "mobile" },
                    { "data": "address" },
                    {
                        "data": "id",
                        "render": function (data) {
                            return '<a href="/Employee/Update/' + data + '">Edit</a> | <a href="/Employee/Delete/' + data + '">Delete</a>';
                        }
                    }
                ]
            });
        });
    </script>*@



